% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_study.R
\name{prepare_study}
\alias{prepare_study}
\alias{prepare_study_data}
\alias{prepare_study_from_eset}
\title{Prepare an scMINER study: emit graph-import layout and/or HDF5 bundle.}
\usage{
prepare_study_data(
  out_dir,
  meta,
  cells,
  clusters = NULL,
  genes,
  expression = NULL,
  activity_tf = NULL,
  activity_sig = NULL,
  network_tf = NULL,
  network_sig = NULL,
  default_genes = NULL,
  emit = c("graph", "bundle"),
  verbose = FALSE
)

prepare_study_from_eset(
  out_dir,
  expression_eset,
  activity_eset = NULL,
  networks_path = NULL,
  meta,
  cell_id_col = "cellID",
  cell_type_col = "cellGroup",
  cell_group_col = "cellGroup",
  coordinate_col = "UMAP",
  gene_symbol_col = "geneSymbol",
  clusters = NULL,
  emit = c("graph", "bundle"),
  verbose = FALSE
)

prepare_study(config_path, emit = c("graph", "bundle"), verbose = FALSE)
}
\arguments{
\item{out_dir}{Output root directory.}

\item{meta}{Named list with `studyID`, `studyAbbr`, `longTitle`,
`shortTitle`, `species`, `coordinate`.}

\item{cells}{data.frame: `cellID`, `cellType`, `cellGroup` (optional),
`coord1`, `coord2`.}

\item{clusters}{data.frame: `cellType`, `count` (optional, computed
from `cells` if absent), `color`, `label_1`, `label_2`.}

\item{genes}{Character vector of gene symbols.}

\item{expression}{Optional `Matrix` (G x N).}

\item{activity_tf, activity_sig}{Optional matrices, same orientation.}

\item{network_tf, network_sig}{Optional data.frames.}

\item{emit}{Character vector — any subset of `c("graph", "bundle")`.}

\item{verbose}{Logical; emit progress messages while writing shards.}

\item{expression_eset}{A Biobase ExpressionSet for the expression matrix.
`pData(eset)` provides the cell metadata; `fData(eset)` provides gene
symbols; `exprs(eset)` provides the value matrix.}

\item{activity_eset}{Optional Biobase ExpressionSet for the activity
matrix. Row names must end in `_TF` / `.TF` or `_SIG` / `.SIG`; rows
are split into `activity_tf` and `activity_sig` accordingly.}

\item{networks_path}{Optional path to a tab-separated networks file with
columns `source, target, NetworkType, [studyID,] CellGroup, mi,
pearson, spearman, rho, pvalue` (the format consumed by the original
`h_networks.R`).}

\item{cell_id_col, cell_type_col, cell_group_col, coordinate_col}{Column names within `pData(expression_eset)`.}

\item{gene_symbol_col}{Column name within `fData(expression_eset)`.}

\item{config_path}{Path to a YAML config.}
}
\description{
Three entry points:
}
\details{
\itemize{
  \item [prepare_study_data()] — lowest-level form; takes already-extracted
    `meta`, `cells`, `clusters`, `genes`, sparse matrices, and network
    data frames. No Biobase / yaml dependency.
  \item [prepare_study_from_eset()] — accepts a Biobase ExpressionSet
    (one for expression, optional one for activity) and pulls out
    pData / fData / exprs internally. Requires the `Biobase` package
    (suggested).
  \item [prepare_study()] — drop-in for the original `main.R`: reads a
    YAML config, loads the referenced RDS / TSV files, then calls
    `prepare_study_from_eset`. Requires the `yaml` and `Biobase`
    packages.
}

All three accept `emit = c("graph", "bundle")`; either or both. The
bundle is always written as `<out_dir>/<studyID>.scminer.h5`.

Internally, `prepare_study_from_eset()` is a thin wrapper
over the staged helpers [extract_cells()], [extract_genes()],
[extract_expression()], [extract_activity()] and [read_networks()].
Call those directly when you want to inspect or debug a single
stage in isolation.

`prepare_study()` is a thin wrapper that calls
[load_study_config()] to parse + validate the YAML, then `readRDS()`
on the input files, then [prepare_study_from_eset()]. Call those
two pieces directly when you want to inspect the parsed config or
the loaded ExpressionSets before writing anything to disk.
}
