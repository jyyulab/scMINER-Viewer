% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_bundle.R
\name{gene_values}
\alias{gene_values}
\title{Read one gene's values from the shard tree.}
\usage{
gene_values(study, gene, relationship = "Express_normalized")
}
\arguments{
\item{study}{An `scminer_study` returned by [load_study()].}

\item{gene}{Gene symbol.}

\item{relationship}{One of `"Express_normalized"` (expression),
`"Activity_tf"`, `"Activity_sig"`.}
}
\value{
Numeric vector of length `nrow(study$cells)`, or `NULL` if
  the gene isn't available for the requested relationship.
}
\description{
Lazily reads `<gene>.csv.gz` from the on-disk shard tree, parses it,
and aligns the values to `study$cells$cellID` (cells missing from the
shard header are returned as `NA`). Results are cached on the study
object so repeated calls for the same gene are free.
}
