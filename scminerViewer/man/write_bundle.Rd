% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_bundle.R
\name{write_bundle}
\alias{write_bundle}
\title{Write a self-contained scMINER study metadata bundle (HDF5).}
\usage{
write_bundle(
  bundle_path,
  meta,
  cells,
  clusters,
  genes,
  expression_genes = NULL,
  activity_tf_genes = NULL,
  activity_sig_genes = NULL,
  default_genes = NULL,
  network_tf = NULL,
  network_sig = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{bundle_path}{Output path; should end in `.scminer.h5`.}

\item{meta}{Named list with `studyID`, `studyAbbr`, `longTitle`,
`shortTitle`, `species`, `coordinate`.}

\item{cells}{data.frame with columns `cellID`, `cellType`, `cellGroup`
(optional, defaults to `cellType`), `coord1`, `coord2`.}

\item{clusters}{data.frame with columns `cellType`, `count`, `color`
(optional), `label_1` (optional), `label_2` (optional).}

\item{genes}{Master character vector of gene symbols (all genes in
the study, regardless of which matrices they appear in).}

\item{expression_genes, activity_tf_genes, activity_sig_genes}{Optional
character vectors listing the genes that have shards in each matrix.
Typically the `GeneSymbol` column of the corresponding manifest CSV.}

\item{default_genes}{Optional character vector; if present, the app
pre-selects these genes on startup.}

\item{network_tf, network_sig}{Optional data.frames.}

\item{overwrite}{Logical; allow overwriting `bundle_path`.}
}
\value{
`bundle_path` (invisibly).
}
\description{
The bundle is a small `.scminer.h5` file containing study metadata,
cell + cluster info, the master gene list, networks, and per-matrix
**gene indexes** (which genes are available in `expression` /
`activity_tf` / `activity_sig`). Expression and activity matrix
values are *not* stored in the bundle — they are read on demand from
the on-disk shard tree by [gene_values()].
}
