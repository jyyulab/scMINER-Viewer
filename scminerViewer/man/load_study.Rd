% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_bundle.R
\name{load_study}
\alias{load_study}
\title{Load a scMINER study bundle from HDF5.}
\usage{
load_study(bundle_path, shard_dir = NULL)
}
\arguments{
\item{bundle_path}{Path to a `.scminer.h5` file written by
[write_bundle()].}

\item{shard_dir}{Directory containing the `expression_files/<studyID>/`
and `activity_files/<studyID>/` shard trees. Defaults to
`dirname(bundle_path)` (i.e. the bundle is co-located with its
shards).}
}
\value{
An S3 object of class `scminer_study`.
}
\description{
Returns an S3 `scminer_study` object with study metadata, cell +
cluster info, the master gene list, per-matrix gene indexes, networks,
and a path to the shard tree from which [gene_values()] reads
individual gene shards on demand.
}
