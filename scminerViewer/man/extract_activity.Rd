% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_study.R
\name{extract_activity}
\alias{extract_activity}
\title{Extract activity matrices (TF + SIG) from an `ExpressionSet`.}
\usage{
extract_activity(activity_eset, master_genes)
}
\arguments{
\item{activity_eset}{A Biobase `ExpressionSet` whose row names carry
the `_TF` / `_SIG` suffixes.}

\item{master_genes}{Character vector — the bundle's master gene
list. Both output matrices have this many rows in this order.}
}
\value{
A list with elements `tf` and `sig`, each either a sparse
  `Matrix` (rows in `master_genes` order) or `NULL` if no rows of
  that kind were present.
}
\description{
Splits rows by suffix (`_TF` / `.TF` → TF; `_SIG` / `.SIG` → SIG),
strips the suffix from row names, and reindexes both sub-matrices
onto the master `genes` list (rows not present in the activity
source become zero rows).
}
