% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_study.R
\name{extract_cells}
\alias{extract_cells}
\title{Extract the cells data.frame from an `ExpressionSet`.}
\usage{
extract_cells(
  expression_eset,
  cell_id_col = "cellID",
  cell_type_col = "cellGroup",
  cell_group_col = cell_type_col,
  coordinate_col = "UMAP"
)
}
\arguments{
\item{expression_eset}{A Biobase `ExpressionSet`.}

\item{cell_id_col, cell_type_col, cell_group_col}{Column names in
`pData(expression_eset)`. `cell_id_col` defaults to the eset's
rownames if not already a column.}

\item{coordinate_col}{Stem of the coord columns; the eset must have
`<coordinate_col>_1` and `<coordinate_col>_2`.}
}
\value{
data.frame with columns `cellID`, `cellType`, `cellGroup`,
  `coord1`, `coord2`.
}
\description{
Pulls `pData()`, coerces factor columns to character, and selects /
renames the cellID, cellType, cellGroup, and coordinate columns into
the canonical `cells` data.frame consumed by [prepare_study_data()].
}
\details{
Errors loudly if any required pData column is missing — useful when
debugging mis-named source columns.
}
