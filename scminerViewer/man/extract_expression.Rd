% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_study.R
\name{extract_expression}
\alias{extract_expression}
\title{Extract the expression matrix from an `ExpressionSet`.}
\usage{
extract_expression(expression_eset, genes = NULL)
}
\arguments{
\item{expression_eset}{A Biobase `ExpressionSet`.}

\item{genes}{Optional character vector; replaces the matrix's
rownames if `nrow(exprs(eset)) == length(genes)`.}
}
\value{
A `dgCMatrix` of shape `(nrow(eset), ncol(eset))`.
}
\description{
Returns a CSC sparse `Matrix` (genes × cells). If `genes` is given,
the matrix's rownames are set from it — useful when the eset's
fData has more useful symbols than the matrix's existing rownames.
}
