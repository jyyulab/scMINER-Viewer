% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Launch the scMINER Viewer Shiny app.}
\usage{
run_app(
  bundle_path,
  shard_dir = NULL,
  host = "127.0.0.1",
  port = NULL,
  launch_browser = interactive(),
  ...
)
}
\arguments{
\item{bundle_path}{Path to a `.scminer.h5` bundle (produced by
[write_bundle()] or [prepare_study_data()]).}

\item{shard_dir}{Directory containing the per-gene shard tree
(`expression_files/<studyID>/`, `activity_files/<studyID>/`). If
`NULL` (default), uses `dirname(bundle_path)`. Pass an explicit
path when the bundle is not co-located with its shards.}

\item{host}{Host interface to bind to. Defaults to `"127.0.0.1"`.}

\item{port}{Port to bind to. Defaults to a random free port (NULL).}

\item{launch_browser}{Logical; open the system browser. Default is
`interactive()`.}

\item{...}{Additional arguments passed to [shiny::runApp()].}
}
\value{
Called for side effect (launches the Shiny app). Returns
  `invisible(NULL)`.
}
\description{
Reads the bundle once at startup and serves the UI matching the layout
at <https://scminer.stjude.org/study/Tregs> (core plots only — see the
implementation plan for what's intentionally out of scope in v1).
}
